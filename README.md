# homebrew-tap

Homebrew formulae for my tools.

```sh
brew install matthewvilaysack/tap/hotword
```

After a hotword release, `scripts/update-formula.sh X.Y.Z` rewrites the formula from that release's checksums; commit and push the result.
